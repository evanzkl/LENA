; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:freenove_esp32_s3_wroom]
platform = espressif32
board = freenove_esp32_s3_wroom
framework = arduino

monitor_speed = 115200
upload_speed = 460800
board_build.partitions = partitions.csv
; Native USB-CDC has no UART bridge auto-reset circuit; letting the monitor
; toggle DTR/RTS on open can hold the chip in a non-booting state.
# monitor_rts = 0
# monitor_dtr = 0
; Board defaults to ARDUINO_USB_CDC_ON_BOOT=0, so Serial never reaches the USB port.
build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1